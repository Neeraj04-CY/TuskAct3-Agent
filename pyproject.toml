[project]
name = "eikon_engine"
version = "0.2.0"
description = "EIKON Engine browser-agent framework"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

